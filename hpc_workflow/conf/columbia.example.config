// Copy this file outside the repository, edit it for the target allocation,
// and pass it with: -c /path/to/columbia.config
process {
    // queue = 'cpu'
    // clusterOptions = '--account=<allocation> --constraint=<constraint>'
}
